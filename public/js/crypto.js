const idArray = () => {
    idArr = [];
    for (i=0; i < 51; i++) {
        idArr.push(data[i].id) 
    }
}

const symSet = (sym) => {
    symArr = [];
    symArr.push(sym)
}
module.exports = symSet;
